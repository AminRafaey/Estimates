import SurfaceIcon from './icon.svg';
import PropTypes from 'prop-types';

SurfaceIcon.defaultProps = {
  color: '#000000',
};

SurfaceIcon.propTypes = {
  color: PropTypes.string,
};

export default SurfaceIcon;
