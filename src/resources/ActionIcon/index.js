import ActionIcon from './icon.svg';
import PropTypes from 'prop-types';

ActionIcon.defaultProps = {
  color: '#000000',
};

ActionIcon.propTypes = {
  color: PropTypes.string,
};

export default ActionIcon;
