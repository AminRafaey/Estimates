import MeasurmentIcon from './icon.svg';
import PropTypes from 'prop-types';

MeasurmentIcon.defaultProps = {
  color: '#000000',
};

MeasurmentIcon.propTypes = {
  color: PropTypes.string,
};

export default MeasurmentIcon;
