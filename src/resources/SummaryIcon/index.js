import SummaryIcon from './icon.svg';
import PropTypes from 'prop-types';

SummaryIcon.defaultProps = {
  color: '#000000',
};

SummaryIcon.propTypes = {
  color: PropTypes.string,
};

export default SummaryIcon;
